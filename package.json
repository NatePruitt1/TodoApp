{
    "name": "todo-app",
    "version": "0.0.0",
    "private": true,
    "packageManager": "npm@11.16.0",
    "workspaces": [
        "apps/*"
    ],
    "scripts": {
        "build": "turbo build",
        "dev": "turbo run dev --parallel",
        "test": "turbo test",
        "lint": "turbo lint"
    },
    "devDependencies": {
        "turbo": "^2.10.3"
    },
    "dependencies": {
        "vite": "^8.1.4"
    }
}

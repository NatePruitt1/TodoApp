{
    "name": "todo-app",
    "version": "0.0.0",
    "private": true,
    "packageManager": "npm@11.16.0",
    "workspaces": [
        "apps/*"
    ],
    "scripts": {
        "build": "turbo build",
        "dev": "turbo run dev --parallel",
        "test": "turbo test",
        "lint": "turbo lint",
        "dropdb": "migrate -path apps/backend/internal/db/migrations -database 'postgresql://postgres:Wallah54639!@localhost:5432/kanban' down",
        "migrate": "migrate -path apps/backend/internal/db/migrations -database 'postgresql://postgres:Wallah54639!@localhost:5432/kanban' up"
    },
    "devDependencies": {
        "turbo": "^2.10.3"
    },
    "dependencies": {
        "vite": "^8.1.4"
    }
}

name: backend-go-test

on:
    push:
        branches:
            - "*"

jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v4
            
            - name: Set up Go
              uses: actions/setup-go@v4
              with:
                go-version: '1.26.x'
            
            - name: Install dependencies
              run: go mod tidy
            
            - name: Run tests
              run: go test -v ./...
            
            - name: Generate coverage report
              run: go test -coverageprofile:coverage.out ./...
            